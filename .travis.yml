language: node_js
node_js:
  - "lts/*"
cache:
  yarn: true
  directories:
    - node_modules

env:
  - "PUBLIC_URL=/eatsheet"

script:
  - yarn test
  - yarn build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: test-travis-ci
